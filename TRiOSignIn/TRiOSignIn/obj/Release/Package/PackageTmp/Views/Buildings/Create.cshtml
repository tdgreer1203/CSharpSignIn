@model TRiOSignIn.Models.Building
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add Buliding</h2>

<br/>

@using (Html.BeginForm("Save", "Buildings"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.MapNumber)
        @Html.TextBoxFor(m => m.MapNumber, new { @class = "form-control" })
    </div>
    @Html.HiddenFor(m => m.Id)
    <button type="submit" class="btn btn-info" id="detailBtn">Save</button>
    <a href="@Url.Action("Index", "Buildings")" class="btn btn-danger" id="detailBtn">CANCEL</a>
}

